# Copy to .env and fill as needed. For local dev, all are optional.

# Server port (default 3000)
PORT=3000

# OAuth / Login (required for dashboard login)
# OAUTH_SERVER_URL=
# VITE_APP_ID=
# JWT_SECRET=

# MongoDB (required for app data – users, projects, apiKeys, usageLogs)
# MONGODB_URI=mongodb+srv://user:pass@cluster.xxxx.mongodb.net/alexza

# Frontend: backend API base URL (tRPC). Local dev fallback = http://localhost:3000.
# Production (e.g. Render): set to your backend URL. Do not commit real URL.
# VITE_API_BASE_URL=https://<render-backend>.onrender.com
