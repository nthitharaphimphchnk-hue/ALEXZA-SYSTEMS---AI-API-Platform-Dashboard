export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// 🚧 OAuth TEMPORARILY DISABLED – no new URL() to avoid invalid URL crash. Re-enable when OAuth is configured.
export const getLoginUrl = (): string | null => {
  return null;
};
