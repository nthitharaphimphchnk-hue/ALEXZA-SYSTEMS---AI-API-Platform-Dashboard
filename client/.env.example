# Copy to .env.local (or .env) for local frontend dev. Do not commit real URLs.

# Backend API base URL (tRPC).
# - Local dev (frontend 5173, backend 3000): use fallback or set below.
# - Production (e.g. frontend on Render/CDN, backend on Render): set to backend URL.
# Example (replace with your Render backend URL, do not commit real value):
# VITE_API_BASE_URL=https://<render-backend>.onrender.com
